name: "Pull Request Labeler"
on:
  pull_request_target:
  schedule:
    - cron: "0 1 * * 1"
jobs:
  labeler:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@v5
